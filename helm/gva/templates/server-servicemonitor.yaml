apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: gva-server-monitor
  namespace: {{ .Values.namespace }}
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: gva-server
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
