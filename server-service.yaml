apiVersion: v1
kind: Service

metadata:
  name: server
  namespace: gva

spec:
  type: ClusterIP

  selector:
    app: gva-server

  ports:
    - name: http
      protocol: TCP
      port: 8888
      targetPort: 8888